diff --git a/dist/index.cjs b/dist/index.cjs
index e953bd5..b0140dd 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -1239,7 +1239,7 @@ var E2BSandbox = class E2BSandbox extends _mastra_core_workspace.MastraSandbox {
 	isSandboxDeadError(error) {
 		if (!error) return false;
 		const errorStr = String(error);
-		return errorStr.includes("sandbox was not found") || errorStr.includes("Sandbox is probably not running") || errorStr.includes("Sandbox not found") || errorStr.includes("sandbox has been killed");
+		return /\bsandbox\b[^\n]{0,48}?\bnot found\b/i.test(errorStr) || errorStr.includes("Sandbox is probably not running") || errorStr.includes("sandbox has been killed");
 	}
 	/**
 	* Handle sandbox timeout by clearing the instance and resetting state.
diff --git a/dist/index.js b/dist/index.js
index b5b7cd5..58e86a0 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1238,7 +1238,7 @@ var E2BSandbox = class E2BSandbox extends MastraSandbox {
 	isSandboxDeadError(error) {
 		if (!error) return false;
 		const errorStr = String(error);
-		return errorStr.includes("sandbox was not found") || errorStr.includes("Sandbox is probably not running") || errorStr.includes("Sandbox not found") || errorStr.includes("sandbox has been killed");
+		return /\bsandbox\b[^\n]{0,48}?\bnot found\b/i.test(errorStr) || errorStr.includes("Sandbox is probably not running") || errorStr.includes("sandbox has been killed");
 	}
 	/**
 	* Handle sandbox timeout by clearing the instance and resetting state.
