diff --git a/dist/agent-BOdDHE12.cjs b/dist/agent-BOdDHE12.cjs
index 6c7efc0..1e979d3 100644
--- a/dist/agent-BOdDHE12.cjs
+++ b/dist/agent-BOdDHE12.cjs
@@ -13129,11 +13129,23 @@ var TokenLimiterProcessor = class TokenLimiterProcessor {
 				} else if (invocation.state === "result") {
 					toolResultCount++;
 					if (invocation.result !== void 0) if (typeof invocation.result === "string") tokenString += invocation.result;
+					else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+						const mediaType = invocation.result.mediaType;
+						const bySize = Math.round(Math.floor(invocation.result.data.length * 3 / 4) / 750);
+						overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
+					}
 					else {
 						tokenString += JSON.stringify(invocation.result);
 						overhead -= 12;
 					}
 				}
+			} else if (part.type === "file") {
+				const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+				tokenString += `${part.filename ?? ""}${mediaType}`;
+				const url = typeof part.url === "string" ? part.url : "";
+				const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+				const bySize = Math.round(Math.floor(base64Length * 3 / 4) / 750);
+				overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
 			} else tokenString += JSON.stringify(part);
 		}
 		overhead += TokenLimiterProcessor.TOKENS_PER_MESSAGE;
diff --git a/dist/agent-CKAVuxKN.js b/dist/agent-CKAVuxKN.js
index 2d9462a..dd28b35 100644
--- a/dist/agent-CKAVuxKN.js
+++ b/dist/agent-CKAVuxKN.js
@@ -13127,11 +13127,23 @@ var TokenLimiterProcessor = class TokenLimiterProcessor {
 				} else if (invocation.state === "result") {
 					toolResultCount++;
 					if (invocation.result !== void 0) if (typeof invocation.result === "string") tokenString += invocation.result;
+					else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+						const mediaType = invocation.result.mediaType;
+						const bySize = Math.round(Math.floor(invocation.result.data.length * 3 / 4) / 750);
+						overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
+					}
 					else {
 						tokenString += JSON.stringify(invocation.result);
 						overhead -= 12;
 					}
 				}
+			} else if (part.type === "file") {
+				const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+				tokenString += `${part.filename ?? ""}${mediaType}`;
+				const url = typeof part.url === "string" ? part.url : "";
+				const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+				const bySize = Math.round(Math.floor(base64Length * 3 / 4) / 750);
+				overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
 			} else tokenString += JSON.stringify(part);
 		}
 		overhead += TokenLimiterProcessor.TOKENS_PER_MESSAGE;
diff --git a/dist/background-tasks/types.d.ts b/dist/background-tasks/types.d.ts
index 2b857d5..1c7eab0 100644
--- a/dist/background-tasks/types.d.ts
+++ b/dist/background-tasks/types.d.ts
@@ -175,6 +175,8 @@ export interface ToolBackgroundConfig {
 }
 export type AgentBackgroundToolConfig = boolean | {
     enabled: boolean;
+    /** Whether calls run in the background when the model omits `_background`. Defaults to `enabled`. */
+    defaultEnabled?: boolean;
     timeoutMs?: number;
 };
 export interface AgentBackgroundConfig {
diff --git a/dist/background-tasks-BOCbbAxn.cjs b/dist/background-tasks-BOCbbAxn.cjs
index 27a1c8b..a1f6b62 100644
--- a/dist/background-tasks-BOCbbAxn.cjs
+++ b/dist/background-tasks-BOCbbAxn.cjs
@@ -1101,8 +1101,10 @@ function resolveBackgroundConfig({ llmBgOverrides, toolName, toolConfig, agentCo
 		maxRetries: managerConfig?.defaultRetries?.maxRetries ?? 0
 	};
 	const agentToolConfig = resolveAgentToolConfig(toolName, agentConfig);
+	const eligible = agentToolConfig?.enabled ?? toolConfig?.enabled ?? false;
+	const defaultEnabled = agentToolConfig?.defaultEnabled ?? eligible;
 	return {
-		runInBackground: agentToolConfig?.enabled ?? toolConfig?.enabled ?? false ? llmOverride?.enabled ?? true : false,
+		runInBackground: eligible ? llmOverride?.enabled ?? defaultEnabled : false,
 		timeoutMs: llmOverride?.timeoutMs ?? agentToolConfig?.timeoutMs ?? toolConfig?.timeoutMs ?? managerConfig?.defaultTimeoutMs ?? 3e5,
 		maxRetries: llmOverride?.maxRetries ?? toolConfig?.maxRetries ?? managerConfig?.defaultRetries?.maxRetries ?? 0
 	};
@@ -1156,17 +1158,18 @@ const backgroundOverrideZodSchema = zod_v4.z.object({
 */
 function generateBackgroundTaskSystemPrompt(tools, agentConfig) {
 	const eligibleTools = [];
-	const enableAll = agentConfig?.tools === "all";
 	for (const [toolName, tool] of Object.entries(tools)) {
-		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : typeof agentConfig?.tools?.[toolName] === "boolean" ? agentConfig.tools[toolName] : agentConfig?.tools?.[toolName]?.enabled ?? false;
+		const agentToolConfig = resolveAgentToolConfig(toolName, agentConfig);
+		const eligible = agentToolConfig?.enabled ?? tool.background?.enabled ?? false;
+		if (!eligible) continue;
 		eligibleTools.push({
 			toolName,
 			toolConfig: tool.background,
-			defaultBackground: enableAll ? true : bgEnabledFromAgentConfig ?? tool.background?.enabled ?? false
+			defaultBackground: agentToolConfig?.defaultEnabled ?? eligible
 		});
 	}
 	if (eligibleTools.length === 0) return;
-	return `You have the ability to run certain tools in the background while continuing the conversation. The following tools support background execution:
+	return `The following tools support background execution:
 ${eligibleTools.map((t) => `- ${t.toolName} (default: ${t.defaultBackground ? "background" : "foreground"})`).join("\n")}
 
 For any of these tools, you can include a "_background" field in the tool arguments to override the default:
@@ -1175,10 +1178,10 @@ For any of these tools, you can include a "_background" field in the tool argume
 All fields in "_background" are optional. Only include what you want to override.
 
 Guidelines:
-- Use background execution when the user doesn't need the result immediately, or when you're launching multiple independent tasks.
+- Use background execution for asynchronous work or multiple independent tasks.
 - Use foreground execution when the user is directly waiting for the result and the conversation can't continue without it.
 - If you don't include "_background", the tool's default configuration is used.
-- When a tool runs in the background, you'll receive a placeholder result with a task ID. You can reference this in your response to the user.
+- When a tool runs in the background, its completion is injected automatically. Do not poll it.
 
 IMPORTANT: "_background" field is always an object. The fields in the _background field should be inside the _background object, not outside of it.`;
 }
diff --git a/dist/background-tasks-C4VvnJ3T.js b/dist/background-tasks-C4VvnJ3T.js
index eeea17d..5ca25f3 100644
--- a/dist/background-tasks-C4VvnJ3T.js
+++ b/dist/background-tasks-C4VvnJ3T.js
@@ -1101,8 +1101,10 @@ function resolveBackgroundConfig({ llmBgOverrides, toolName, toolConfig, agentCo
 		maxRetries: managerConfig?.defaultRetries?.maxRetries ?? 0
 	};
 	const agentToolConfig = resolveAgentToolConfig(toolName, agentConfig);
+	const eligible = agentToolConfig?.enabled ?? toolConfig?.enabled ?? false;
+	const defaultEnabled = agentToolConfig?.defaultEnabled ?? eligible;
 	return {
-		runInBackground: agentToolConfig?.enabled ?? toolConfig?.enabled ?? false ? llmOverride?.enabled ?? true : false,
+		runInBackground: eligible ? llmOverride?.enabled ?? defaultEnabled : false,
 		timeoutMs: llmOverride?.timeoutMs ?? agentToolConfig?.timeoutMs ?? toolConfig?.timeoutMs ?? managerConfig?.defaultTimeoutMs ?? 3e5,
 		maxRetries: llmOverride?.maxRetries ?? toolConfig?.maxRetries ?? managerConfig?.defaultRetries?.maxRetries ?? 0
 	};
@@ -1156,17 +1158,18 @@ const backgroundOverrideZodSchema = z.object({
 */
 function generateBackgroundTaskSystemPrompt(tools, agentConfig) {
 	const eligibleTools = [];
-	const enableAll = agentConfig?.tools === "all";
 	for (const [toolName, tool] of Object.entries(tools)) {
-		const bgEnabledFromAgentConfig = agentConfig?.tools === "all" ? false : typeof agentConfig?.tools?.[toolName] === "boolean" ? agentConfig.tools[toolName] : agentConfig?.tools?.[toolName]?.enabled ?? false;
+		const agentToolConfig = resolveAgentToolConfig(toolName, agentConfig);
+		const eligible = agentToolConfig?.enabled ?? tool.background?.enabled ?? false;
+		if (!eligible) continue;
 		eligibleTools.push({
 			toolName,
 			toolConfig: tool.background,
-			defaultBackground: enableAll ? true : bgEnabledFromAgentConfig ?? tool.background?.enabled ?? false
+			defaultBackground: agentToolConfig?.defaultEnabled ?? eligible
 		});
 	}
 	if (eligibleTools.length === 0) return;
-	return `You have the ability to run certain tools in the background while continuing the conversation. The following tools support background execution:
+	return `The following tools support background execution:
 ${eligibleTools.map((t) => `- ${t.toolName} (default: ${t.defaultBackground ? "background" : "foreground"})`).join("\n")}
 
 For any of these tools, you can include a "_background" field in the tool arguments to override the default:
@@ -1175,10 +1178,10 @@ For any of these tools, you can include a "_background" field in the tool argume
 All fields in "_background" are optional. Only include what you want to override.
 
 Guidelines:
-- Use background execution when the user doesn't need the result immediately, or when you're launching multiple independent tasks.
+- Use background execution for asynchronous work or multiple independent tasks.
 - Use foreground execution when the user is directly waiting for the result and the conversation can't continue without it.
 - If you don't include "_background", the tool's default configuration is used.
-- When a tool runs in the background, you'll receive a placeholder result with a task ID. You can reference this in your response to the user.
+- When a tool runs in the background, its completion is injected automatically. Do not poll it.
 
 IMPORTANT: "_background" field is always an object. The fields in the _background field should be inside the _background object, not outside of it.`;
 }
