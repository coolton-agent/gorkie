diff --git a/dist/agent-BOdDHE12.cjs b/dist/agent-BOdDHE12.cjs
index 6c7efc0..32fd149 100644
--- a/dist/agent-BOdDHE12.cjs
+++ b/dist/agent-BOdDHE12.cjs
@@ -13129,11 +13129,23 @@ var TokenLimiterProcessor = class TokenLimiterProcessor {
 				} else if (invocation.state === "result") {
 					toolResultCount++;
 					if (invocation.result !== void 0) if (typeof invocation.result === "string") tokenString += invocation.result;
+					else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+						const mediaType = invocation.result.mediaType;
+						const bySize = Math.round(Math.floor(invocation.result.data.length * 3 / 4) / 750);
+						overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
+					}
 					else {
 						tokenString += JSON.stringify(invocation.result);
 						overhead -= 12;
 					}
 				}
+			} else if (part.type === "file") {
+				const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+				tokenString += `${part.filename ?? ""}${mediaType}`;
+				const url = typeof part.url === "string" ? part.url : "";
+				const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+				const bySize = Math.round(Math.floor(base64Length * 3 / 4) / 750);
+				overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
 			} else tokenString += JSON.stringify(part);
 		}
 		overhead += TokenLimiterProcessor.TOKENS_PER_MESSAGE;
@@ -19308,7 +19320,12 @@ async function runStreamingDriver({ stream, chatThread, adapter, toolDisplay, to
 			continue;
 		}
 		if (chunk.type === "step-finish") {
-			if (toolDisplay !== "grouped") await closeSession();
+			// A continued step (retry, in-place or fallback-model escalation) means
+			// the run is still alive: closing here would finalize the Slack message
+			// before the retry's output arrives. Mirrors ChatChannelOutputProcessor's
+			// own isContinued check for the same chunk type.
+			const stepIsContinued = chunk.payload?.stepResult?.isContinued === true;
+			if (toolDisplay !== "grouped" && !stepIsContinued) await closeSession();
 			continue;
 		}
 		if (chunk.type === "file") {
@@ -19321,11 +19338,11 @@ async function runStreamingDriver({ stream, chatThread, adapter, toolDisplay, to
 			continue;
 		}
 		if (chunk.type === "finish") {
-			await closeSession();
 			tracker.reset();
 			continue;
 		}
 		if (chunk.type === "error") {
+			logger?.warn?.(`[fallback-debug] runId='${chunk.runId}' runStreamingDriver RECEIVED error chunk from fullStream, closing session and posting`);
 			await closeSession();
 			await postStreamError({
 				chunk,
@@ -24662,7 +24679,7 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 							...options,
 							messageId: currentMessageId
 						}) } : void 0;
-						if ((await processorRunner.runProcessAPIError({
+						const syncErrorResult = await processorRunner.runProcessAPIError({
 							error,
 							messages: messageList.get.all.db(),
 							messageList,
@@ -24679,7 +24696,9 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 								outputStream.messageId = currentMessageId;
 								return currentMessageId;
 							}
-						})).retry && canRetryError) {
+						});
+						logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: model=${modelIdStr} activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length} canRetryError=${canRetryError} syncErrorResultRetry=${syncErrorResult.retry} currentRetryCount=${currentRetryCount} maxErrorProcessorRetries=${maxErrorProcessorRetries}`);
+						if (syncErrorResult.retry && canRetryError) {
 							runState.setState({
 								hasErrored: false,
 								apiError: void 0
@@ -24693,6 +24712,26 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 								processAPIErrorRetry: { retry: true }
 							};
 						}
+						// Retries on this model are exhausted (or the processor declined). Before
+						// giving up entirely, fall through to the next fallback model instead of
+						// throwing, so a model that errors here (not just one that fails to start)
+						// still gets a chance at the rest of the fallback array.
+						if (activeFallbackModelIndex + 1 < models.length) {
+							logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: ESCALATING from index ${activeFallbackModelIndex} to ${activeFallbackModelIndex + 1} (of ${models.length})`);
+							runState.setState({
+								hasErrored: false,
+								apiError: void 0
+							});
+							return {
+								outputStream,
+								callBail: false,
+								runState,
+								stepTools: currentStep.tools,
+								stepWorkspace: currentStep.workspace,
+								processAPIErrorRetry: { retry: true, advanceFallbackModel: true }
+							};
+						}
+						logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: NOT escalating, throwing. activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length}`);
 						throw error;
 					}
 				}
@@ -24791,6 +24830,7 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 						return currentMessageId;
 					}
 				});
+				logger?.warn?.(`[fallback-debug] runId='${runId}' deferred-chunk branch: activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length} canRetryError=${canRetryError} errorResultRetry=${errorResult.retry} currentRetryCount=${currentRetryCount}`);
 				if (errorResult.retry && canRetryError) {
 					apiErrorRetryResult = errorResult;
 					runState.setState({
@@ -24798,6 +24838,16 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 						apiError: void 0,
 						deferredErrorChunk: void 0
 					});
+				} else if (activeFallbackModelIndex + 1 < models.length) {
+					logger?.warn?.(`[fallback-debug] runId='${runId}' deferred-chunk branch: ESCALATING from index ${activeFallbackModelIndex} to ${activeFallbackModelIndex + 1} (of ${models.length})`);
+					// Same escalation as the synchronous-throw path above, for errors
+					// that only surface after the model already started streaming.
+					apiErrorRetryResult = { retry: true, advanceFallbackModel: true };
+					runState.setState({
+						hasErrored: false,
+						apiError: void 0,
+						deferredErrorChunk: void 0
+					});
 				}
 			}
 			if (apiErrorRetryResult?.retry && options?.abortSignal?.aborted) {
@@ -24813,9 +24863,13 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 			}
 			if (apiErrorRetryResult?.retry) {
 				cleanupProviderToolSpans(true);
+				const advancingFallbackModel = apiErrorRetryResult.advanceFallbackModel === true;
+				const nextFallbackModelIndex = advancingFallbackModel ? activeFallbackModelIndex + 1 : activeFallbackModelIndex;
 				const currentProcessorRetryCount = inputData.processorRetryCount || 0;
 				const steps = inputData.output?.steps || [];
-				const nextProcessorRetryCount = currentProcessorRetryCount + 1;
+				// A model switch gets its own fresh retry budget instead of inheriting
+				// the exhausted count from the model it's replacing.
+				const nextProcessorRetryCount = advancingFallbackModel ? 0 : currentProcessorRetryCount + 1;
 				const messages = {
 					all: messageList.get.all.aiV5.model(),
 					user: messageList.get.input.aiV5.model(),
@@ -24843,19 +24897,26 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 					},
 					messages,
 					processorRetryCount: nextProcessorRetryCount,
-					...activeFallbackModelIndex > 0 ? { fallbackModelIndex: activeFallbackModelIndex } : {}
+					...nextFallbackModelIndex > 0 ? { fallbackModelIndex: nextFallbackModelIndex } : {}
 				};
 			}
 			if (runState.state.deferredErrorChunk && runState.state.hasErrored) {
 				const deferredChunk = runState.state.deferredErrorChunk;
 				const deferredError = require_error.getErrorFromUnknown(deferredChunk.payload.error, { fallbackMessage: "Unknown error in agent stream" });
-				require_trip_wire.safeEnqueue(controller, {
+				logger?.warn?.(`[fallback-debug] runId='${runId}' ENQUEUING TERMINAL ERROR CHUNK to controller: ${deferredError?.message}`);
+				const terminalErrorChunk = {
 					...deferredChunk,
 					payload: {
 						...deferredChunk.payload,
 						error: deferredError
 					}
-				});
+				};
+				// Raw per-model-call error chunks are withheld from output processors so a
+				// retryable failure never renders, which makes this the only error the
+				// renderer can see. Route it through the processors, not straight to the
+				// controller, or an exhausted fallback would fail silently.
+				if (outputWriter) await outputWriter(terminalErrorChunk);
+				else require_trip_wire.safeEnqueue(controller, terminalErrorChunk);
 				await options?.onError?.({ error: deferredError });
 				runState.setState({ deferredErrorChunk: void 0 });
 			}
@@ -26790,9 +26851,13 @@ function workflowLoopStream({ resumeContext, requireToolApproval, models, toolCh
 				actor: rest.actor
 			});
 			if (executionResult.status !== "success") {
+				rest.logger?.warn?.(`[fallback-debug] runId='${runId}' workflowLoopStream: executionResult.status='${executionResult.status}' (non-success, closing stream)`);
 				if (executionResult.status === "failed") {
 					const error = require_error.getErrorFromUnknown(executionResult.error, { fallbackMessage: "Unknown error in agent workflow stream" });
-					require_trip_wire.safeEnqueue(controller, {
+					rest.logger?.warn?.(`[fallback-debug] runId='${runId}' WORKFLOW-LEVEL FAILURE in workflowLoopStream: executionResult.status='${executionResult.status}' error='${error?.message}' - this is the dowhile loop's own status check, separate from createLLMExecutionStep's own retry/escalation logic`);
+					// Same reason as the terminal deferred-error chunk: output processors no
+					// longer see raw error chunks, so a sync throw must reach them here.
+					await outputWriter({
 						type: "error",
 						runId,
 						from: "AGENT",
diff --git a/dist/agent-CKAVuxKN.js b/dist/agent-CKAVuxKN.js
index 2d9462a..ae469b3 100644
--- a/dist/agent-CKAVuxKN.js
+++ b/dist/agent-CKAVuxKN.js
@@ -13127,11 +13127,23 @@ var TokenLimiterProcessor = class TokenLimiterProcessor {
 				} else if (invocation.state === "result") {
 					toolResultCount++;
 					if (invocation.result !== void 0) if (typeof invocation.result === "string") tokenString += invocation.result;
+					else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+						const mediaType = invocation.result.mediaType;
+						const bySize = Math.round(Math.floor(invocation.result.data.length * 3 / 4) / 750);
+						overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
+					}
 					else {
 						tokenString += JSON.stringify(invocation.result);
 						overhead -= 12;
 					}
 				}
+			} else if (part.type === "file") {
+				const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+				tokenString += `${part.filename ?? ""}${mediaType}`;
+				const url = typeof part.url === "string" ? part.url : "";
+				const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+				const bySize = Math.round(Math.floor(base64Length * 3 / 4) / 750);
+				overhead += bySize > 0 ? Math.max(200, Math.min(4e3, bySize)) : mediaType.startsWith("image/") ? 765 : 258;
 			} else tokenString += JSON.stringify(part);
 		}
 		overhead += TokenLimiterProcessor.TOKENS_PER_MESSAGE;
@@ -19306,7 +19318,12 @@ async function runStreamingDriver({ stream, chatThread, adapter, toolDisplay, to
 			continue;
 		}
 		if (chunk.type === "step-finish") {
-			if (toolDisplay !== "grouped") await closeSession();
+			// A continued step (retry, in-place or fallback-model escalation) means
+			// the run is still alive: closing here would finalize the Slack message
+			// before the retry's output arrives. Mirrors ChatChannelOutputProcessor's
+			// own isContinued check for the same chunk type.
+			const stepIsContinued = chunk.payload?.stepResult?.isContinued === true;
+			if (toolDisplay !== "grouped" && !stepIsContinued) await closeSession();
 			continue;
 		}
 		if (chunk.type === "file") {
@@ -19319,11 +19336,11 @@ async function runStreamingDriver({ stream, chatThread, adapter, toolDisplay, to
 			continue;
 		}
 		if (chunk.type === "finish") {
-			await closeSession();
 			tracker.reset();
 			continue;
 		}
 		if (chunk.type === "error") {
+			logger?.warn?.(`[fallback-debug] runId='${chunk.runId}' runStreamingDriver RECEIVED error chunk from fullStream, closing session and posting`);
 			await closeSession();
 			await postStreamError({
 				chunk,
@@ -24660,7 +24677,7 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 							...options,
 							messageId: currentMessageId
 						}) } : void 0;
-						if ((await processorRunner.runProcessAPIError({
+						const syncErrorResult = await processorRunner.runProcessAPIError({
 							error,
 							messages: messageList.get.all.db(),
 							messageList,
@@ -24677,7 +24694,9 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 								outputStream.messageId = currentMessageId;
 								return currentMessageId;
 							}
-						})).retry && canRetryError) {
+						});
+						logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: model=${modelIdStr} activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length} canRetryError=${canRetryError} syncErrorResultRetry=${syncErrorResult.retry} currentRetryCount=${currentRetryCount} maxErrorProcessorRetries=${maxErrorProcessorRetries}`);
+						if (syncErrorResult.retry && canRetryError) {
 							runState.setState({
 								hasErrored: false,
 								apiError: void 0
@@ -24691,6 +24710,26 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 								processAPIErrorRetry: { retry: true }
 							};
 						}
+						// Retries on this model are exhausted (or the processor declined). Before
+						// giving up entirely, fall through to the next fallback model instead of
+						// throwing, so a model that errors here (not just one that fails to start)
+						// still gets a chance at the rest of the fallback array.
+						if (activeFallbackModelIndex + 1 < models.length) {
+							logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: ESCALATING from index ${activeFallbackModelIndex} to ${activeFallbackModelIndex + 1} (of ${models.length})`);
+							runState.setState({
+								hasErrored: false,
+								apiError: void 0
+							});
+							return {
+								outputStream,
+								callBail: false,
+								runState,
+								stepTools: currentStep.tools,
+								stepWorkspace: currentStep.workspace,
+								processAPIErrorRetry: { retry: true, advanceFallbackModel: true }
+							};
+						}
+						logger?.warn?.(`[fallback-debug] runId='${runId}' sync-throw branch: NOT escalating, throwing. activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length}`);
 						throw error;
 					}
 				}
@@ -24789,6 +24828,7 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 						return currentMessageId;
 					}
 				});
+				logger?.warn?.(`[fallback-debug] runId='${runId}' deferred-chunk branch: activeFallbackModelIndex=${activeFallbackModelIndex} modelsLength=${models.length} canRetryError=${canRetryError} errorResultRetry=${errorResult.retry} currentRetryCount=${currentRetryCount}`);
 				if (errorResult.retry && canRetryError) {
 					apiErrorRetryResult = errorResult;
 					runState.setState({
@@ -24796,6 +24836,16 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 						apiError: void 0,
 						deferredErrorChunk: void 0
 					});
+				} else if (activeFallbackModelIndex + 1 < models.length) {
+					logger?.warn?.(`[fallback-debug] runId='${runId}' deferred-chunk branch: ESCALATING from index ${activeFallbackModelIndex} to ${activeFallbackModelIndex + 1} (of ${models.length})`);
+					// Same escalation as the synchronous-throw path above, for errors
+					// that only surface after the model already started streaming.
+					apiErrorRetryResult = { retry: true, advanceFallbackModel: true };
+					runState.setState({
+						hasErrored: false,
+						apiError: void 0,
+						deferredErrorChunk: void 0
+					});
 				}
 			}
 			if (apiErrorRetryResult?.retry && options?.abortSignal?.aborted) {
@@ -24811,9 +24861,13 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 			}
 			if (apiErrorRetryResult?.retry) {
 				cleanupProviderToolSpans(true);
+				const advancingFallbackModel = apiErrorRetryResult.advanceFallbackModel === true;
+				const nextFallbackModelIndex = advancingFallbackModel ? activeFallbackModelIndex + 1 : activeFallbackModelIndex;
 				const currentProcessorRetryCount = inputData.processorRetryCount || 0;
 				const steps = inputData.output?.steps || [];
-				const nextProcessorRetryCount = currentProcessorRetryCount + 1;
+				// A model switch gets its own fresh retry budget instead of inheriting
+				// the exhausted count from the model it's replacing.
+				const nextProcessorRetryCount = advancingFallbackModel ? 0 : currentProcessorRetryCount + 1;
 				const messages = {
 					all: messageList.get.all.aiV5.model(),
 					user: messageList.get.input.aiV5.model(),
@@ -24841,19 +24895,26 @@ function createLLMExecutionStep({ models, _internal, messageId: messageIdPassed,
 					},
 					messages,
 					processorRetryCount: nextProcessorRetryCount,
-					...activeFallbackModelIndex > 0 ? { fallbackModelIndex: activeFallbackModelIndex } : {}
+					...nextFallbackModelIndex > 0 ? { fallbackModelIndex: nextFallbackModelIndex } : {}
 				};
 			}
 			if (runState.state.deferredErrorChunk && runState.state.hasErrored) {
 				const deferredChunk = runState.state.deferredErrorChunk;
 				const deferredError = getErrorFromUnknown(deferredChunk.payload.error, { fallbackMessage: "Unknown error in agent stream" });
-				safeEnqueue(controller, {
+				logger?.warn?.(`[fallback-debug] runId='${runId}' ENQUEUING TERMINAL ERROR CHUNK to controller: ${deferredError?.message}`);
+				const terminalErrorChunk = {
 					...deferredChunk,
 					payload: {
 						...deferredChunk.payload,
 						error: deferredError
 					}
-				});
+				};
+				// Raw per-model-call error chunks are withheld from output processors so a
+				// retryable failure never renders, which makes this the only error the
+				// renderer can see. Route it through the processors, not straight to the
+				// controller, or an exhausted fallback would fail silently.
+				if (outputWriter) await outputWriter(terminalErrorChunk);
+				else safeEnqueue(controller, terminalErrorChunk);
 				await options?.onError?.({ error: deferredError });
 				runState.setState({ deferredErrorChunk: void 0 });
 			}
@@ -26788,9 +26849,13 @@ function workflowLoopStream({ resumeContext, requireToolApproval, models, toolCh
 				actor: rest.actor
 			});
 			if (executionResult.status !== "success") {
+				rest.logger?.warn?.(`[fallback-debug] runId='${runId}' workflowLoopStream: executionResult.status='${executionResult.status}' (non-success, closing stream)`);
 				if (executionResult.status === "failed") {
 					const error = getErrorFromUnknown(executionResult.error, { fallbackMessage: "Unknown error in agent workflow stream" });
-					safeEnqueue(controller, {
+					rest.logger?.warn?.(`[fallback-debug] runId='${runId}' WORKFLOW-LEVEL FAILURE in workflowLoopStream: executionResult.status='${executionResult.status}' error='${error?.message}' - this is the dowhile loop's own status check, separate from createLLMExecutionStep's own retry/escalation logic`);
+					// Same reason as the terminal deferred-error chunk: output processors no
+					// longer see raw error chunks, so a sync throw must reach them here.
+					await outputWriter({
 						type: "error",
 						runId,
 						from: "AGENT",
diff --git a/dist/trip-wire-CBLOOcnC.cjs b/dist/trip-wire-CBLOOcnC.cjs
index c3e5273..40d9ebd 100644
--- a/dist/trip-wire-CBLOOcnC.cjs
+++ b/dist/trip-wire-CBLOOcnC.cjs
@@ -2874,7 +2874,7 @@ var MastraModelOutput = class extends require_base.MastraBase {
 		if (processorRunner && options.isLLMExecutionStep) {
 			const processorStates = options.processorStates || /* @__PURE__ */ new Map();
 			processedStream = stream.pipeThrough(new stream_web.TransformStream({ async transform(chunk, controller) {
-				if (chunk.type === "finish" && chunk.payload?.stepResult?.reason === "tool-calls") {
+				if (chunk.type === "finish" && chunk.payload?.stepResult?.reason === "tool-calls" || chunk.type === "error" || chunk.type === "finish" && chunk.payload?.stepResult?.reason === "error") {
 					controller.enqueue(chunk);
 					return;
 				} else {
diff --git a/dist/trip-wire-nmFrT3yU.js b/dist/trip-wire-nmFrT3yU.js
index 3a74ec6..1b2a0c2 100644
--- a/dist/trip-wire-nmFrT3yU.js
+++ b/dist/trip-wire-nmFrT3yU.js
@@ -2874,7 +2874,7 @@ var MastraModelOutput = class extends MastraBase {
 		if (processorRunner && options.isLLMExecutionStep) {
 			const processorStates = options.processorStates || /* @__PURE__ */ new Map();
 			processedStream = stream.pipeThrough(new TransformStream({ async transform(chunk, controller) {
-				if (chunk.type === "finish" && chunk.payload?.stepResult?.reason === "tool-calls") {
+				if (chunk.type === "finish" && chunk.payload?.stepResult?.reason === "tool-calls" || chunk.type === "error" || chunk.type === "finish" && chunk.payload?.stepResult?.reason === "error") {
 					controller.enqueue(chunk);
 					return;
 				} else {
